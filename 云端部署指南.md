# Agent Core 云端部署指南

## 您的服务器配置
- **规格**: 2核4GB
- **适用性**: ✅ 适合运行基于API的LLM服务（推荐OpenAI/Claude API）
- **注意**: 不建议运行Ollama本地模型（会占用过多内存）

## 快速部署步骤

### 方案一：使用一键部署脚本（推荐）

1. **上传文件到服务器**
```bash
# 在本地打包
cd C:\Users\23054\Desktop\室内无人机\drone-web-control\agent-core
tar -czf agent-core.tar.gz .

# 上传到服务器
scp agent-core.tar.gz root@your-server-ip:/opt/

# SSH登录服务器
ssh root@your-server-ip

# 解压
cd /opt
tar -xzf agent-core.tar.gz
cd agent-core
```

2. **运行部署脚本**
```bash
sudo bash quick-deploy.sh
```

3. **按提示配置**
- 选择LLM提供商（建议选择 OpenAI）
- 输入API密钥
- 输入无人机后端地址（如果在同一服务器：`http://localhost:3001`）

4. **完成！**
脚本会自动完成以下工作：
- ✅ 安装Docker和Docker Compose
- ✅ 配置环境变量
- ✅ 构建Docker镜像
- ✅ 配置防火墙
- ✅ 启动服务
- ✅ 健康检查

### 方案二：Railway平台部署（零运维）

1. **推送代码到GitHub**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/agent-core.git
git push -u origin main
```

2. **在Railway部署**
- 访问 https://railway.app/
- 连接GitHub仓库
- Railway会自动检测Dockerfile并部署
- 在环境变量中配置：
  ```
  LLM_PROVIDER=openai
  LLM_MODEL=gpt-3.5-turbo
  OPENAI_API_KEY=your-key-here
  BACKEND_URL=your-drone-backend-url
  ```

## API密钥获取

### OpenAI（推荐）
1. 访问 https://platform.openai.com/
2. 注册/登录账号
3. 进入 API Keys 页面
4. 创建新密钥
5. **模型推荐**：
   - `gpt-3.5-turbo`: 便宜快速（$0.0015/1K tokens）
   - `gpt-4o-mini`: 平衡性能与成本
   - `gpt-4o`: 最强性能

### Claude（如果偏好使用）
**注意**：您当前的 `cr_` 开头的密钥是Claude Code专用密钥，无法用于API调用。

需要从以下方式之一获取正式API密钥：
1. **Anthropic官方**: https://console.anthropic.com/
2. **您的第三方服务商**: https://claude.micu.wiki/（需要确认是否提供标准API密钥）

### Ollama（本地免费方案）
**不推荐在2核4GB服务器上使用**，但如果想尝试：
```bash
# 在服务器上安装
curl https://ollama.ai/install.sh | sh

# 下载模型（会占用较多内存）
ollama pull llama2

# 配置环境变量
LLM_PROVIDER=ollama
LLM_MODEL=llama2
```

## 部署后的访问

### 检查服务状态
```bash
# 查看运行状态
docker-compose -f docker-compose.prod.yml ps

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f

# 查看资源占用
docker stats
```

### 访问地址
```
API服务: http://your-server-ip:8000
API文档: http://your-server-ip:8000/docs
健康检查: http://your-server-ip:8000/api/agent/health
```

### 测试API
```bash
curl -X POST http://your-server-ip:8000/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好", "session_id": "test123"}'
```

## 常见问题

### Q1: 部署后服务无法访问
**检查**：
```bash
# 1. 检查服务是否运行
docker ps

# 2. 检查日志
docker-compose -f docker-compose.prod.yml logs

# 3. 检查防火墙
ufw status

# 4. 检查云服务器安全组
# 需要在云服务商控制台开放8000端口
```

### Q2: API调用失败
**可能原因**：
1. API密钥错误或失效
2. API额度用完
3. 网络问题（如需要代理）

**解决**：
```bash
# 修改.env文件
vim .env

# 重启服务
docker-compose -f docker-compose.prod.yml restart
```

### Q3: 内存不足
**优化方案**：
```bash
# 1. 限制Docker容器内存
# 在docker-compose.prod.yml中添加：
services:
  agent-core:
    mem_limit: 2g
    mem_reservation: 1g

# 2. 监控资源使用
docker stats

# 3. 如果使用Ollama，切换到API方案
```

### Q4: 如何更新服务
```bash
# 1. 拉取最新代码
git pull

# 2. 重新构建
docker-compose -f docker-compose.prod.yml build

# 3. 重启服务
docker-compose -f docker-compose.prod.yml up -d
```

## 与前端集成

### WebSocket连接
```javascript
// 在您的React前端中
const ws = new WebSocket('ws://your-server-ip:8000/ws/chat');

ws.onopen = () => {
  ws.send(JSON.stringify({
    message: "起飞到2米高度",
    session_id: "user123"
  }));
};

ws.onmessage = (event) => {
  const response = JSON.parse(event.data);
  console.log('Agent响应:', response);
};
```

### HTTP API调用
```javascript
// REST API方式
async function chatWithAgent(message) {
  const response = await fetch('http://your-server-ip:8000/api/agent/chat', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      message: message,
      session_id: sessionStorage.getItem('agentSessionId')
    })
  });
  return await response.json();
}
```

## 成本估算

### 使用OpenAI API（推荐）
- **服务器成本**: ¥30-50/月（2核4GB云服务器）
- **API成本**:
  - gpt-3.5-turbo: ~¥0.01/次对话
  - gpt-4o-mini: ~¥0.05/次对话
  - gpt-4o: ~¥0.3/次对话
- **月总成本**: ¥50-100（轻度使用）

### 使用Railway部署
- **成本**: $5-10/月（包含服务器+部署）
- **API成本**: 同上
- **优势**: 零运维，自动扩容

### 使用Ollama本地模型
- **服务器成本**: ¥60-100/月（需要4核8GB+）
- **API成本**: ¥0（完全免费）
- **劣势**: 响应质量较低，占用资源大

## 推荐配置

**最佳性价比方案**：
```
服务器: 2核4GB阿里云/腾讯云轻量服务器
LLM: OpenAI gpt-3.5-turbo
部署: Docker Compose
总成本: ~¥50-80/月
```

**零运维方案**：
```
平台: Railway
LLM: OpenAI gpt-4o-mini
总成本: ~$10-15/月
```

## 安全建议

1. **启用防火墙**
```bash
ufw enable
ufw allow 22/tcp    # SSH
ufw allow 8000/tcp  # Agent API
```

2. **配置HTTPS**（可选，使用Nginx）
```bash
# 在docker-compose.prod.yml中启用nginx profile
docker-compose -f docker-compose.prod.yml --profile with-nginx up -d
```

3. **API认证**（可选）
在`.env`中添加：
```bash
API_KEY=your-random-secret-key
ALLOWED_ORIGINS=https://your-frontend-domain.com
```

4. **定期备份**
```bash
# 备份配置
cp .env .env.backup

# 备份日志
docker-compose -f docker-compose.prod.yml logs > logs-$(date +%Y%m%d).txt
```

## 下一步

1. ✅ 准备API密钥（OpenAI推荐）
2. ✅ 选择部署方式（一键脚本 or Railway）
3. ✅ 执行部署
4. ✅ 测试API连接
5. ✅ 集成到前端
6. ✅ 监控运行状态

---

**需要帮助？**
- 查看详细文档: [README.md](README.md)
- OpenAI设置: [QUICKSTART.md](QUICKSTART.md)
- Claude设置: [CLAUDE_SETUP.md](CLAUDE_SETUP.md)
- 完整部署指南: [CLOUD_DEPLOYMENT.md](CLOUD_DEPLOYMENT.md)
