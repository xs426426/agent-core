# 部署到您的阿里云服务器

## 服务器信息
- **IP地址**: 8.136.43.216
- **系统**: Ubuntu 22.04
- **配置**: 2核4GB

## 快速部署步骤

### 第一步：准备文件

在您当前的Windows电脑上执行：

```powershell
# 1. 打包agent-core文件夹
cd C:\Users\23054\Desktop\室内无人机\drone-web-control
tar -czf agent-core.tar.gz agent-core\

# 2. 上传到服务器（使用scp或SFTP工具）
scp agent-core.tar.gz root@8.136.43.216:/opt/
```

**或者使用WinSCP等图形化工具**：
1. 下载WinSCP: https://winscp.net/
2. 连接到 `8.136.43.216`
3. 将整个 `agent-core` 文件夹拖到服务器的 `/opt/` 目录

### 第二步：SSH登录服务器

```bash
ssh root@8.136.43.216
```

### 第三步：解压并部署

```bash
# 进入目录
cd /opt

# 如果是tar.gz文件
tar -xzf agent-core.tar.gz

# 进入agent-core目录
cd agent-core

# 赋予执行权限
chmod +x quick-deploy.sh

# 运行一键部署脚本
sudo bash quick-deploy.sh
```

### 第四步：按提示配置

脚本会问您以下问题：

```
请选择LLM提供商:
  1) OpenAI (推荐)
  2) Claude
  3) Ollama (本地，免费但占用资源)
请选择 [1-3]:
```
**输入**: `1` (选择OpenAI)

```
请输入模型名称 [gpt-3.5-turbo]:
```
**输入**: 直接回车（使用默认的gpt-3.5-turbo）

```
请输入OpenAI API密钥:
```
**输入**: 您的OpenAI API Key（如 `sk-proj-xxxxx`）

```
无人机后端地址 [http://localhost:3001]:
```
**输入**: `http://localhost:3001`（如果无人机后端也在同一服务器）
或输入实际的后端地址

### 第五步：配置阿里云安全组

**重要**：需要在阿里云控制台开放端口

1. 登录阿里云控制台
2. 进入 ECS 实例管理
3. 找到服务器 8.136.43.216
4. 点击"安全组配置"
5. 添加以下入方向规则：

| 协议 | 端口 | 授权对象 | 说明 |
|------|------|----------|------|
| TCP | 8000 | 0.0.0.0/0 | Agent API服务 |
| TCP | 22 | 0.0.0.0/0 | SSH (如果还没开) |
| TCP | 3001 | 127.0.0.1/32 | 无人机后端（仅本机） |

### 第六步：验证部署

```bash
# 1. 检查服务状态
docker-compose -f docker-compose.prod.yml ps

# 应该看到：
# NAME                STATE     PORTS
# agent-core-prod     running   0.0.0.0:8000->8000/tcp

# 2. 检查健康状态
curl http://localhost:8000/api/agent/health

# 应该返回：
# {"status":"healthy","timestamp":"..."}

# 3. 查看日志
docker-compose -f docker-compose.prod.yml logs -f
```

### 第七步：测试访问

**从您的电脑浏览器访问**：

1. **API文档**: http://8.136.43.216:8000/docs
2. **健康检查**: http://8.136.43.216:8000/api/agent/health

**测试对话API**：
```bash
curl -X POST http://8.136.43.216:8000/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好，介绍一下你自己", "session_id": "test123"}'
```

## 如果没有OpenAI API密钥

### 方案A：注册OpenAI（推荐）

1. 访问: https://platform.openai.com/signup
2. 注册账号（需要国外手机号或接码平台）
3. 充值至少 $5
4. 创建API密钥: https://platform.openai.com/api-keys

**费用**:
- gpt-3.5-turbo: $0.0015/1K tokens (约 ¥0.01/次对话)
- gpt-4o-mini: $0.15/1M tokens (约 ¥0.05/次对话)

### 方案B：使用第三方OpenAI代理

国内有一些OpenAI代理服务商，如：
- API2D: https://api2d.com/
- CloseAI: https://closeai.biz/
- OpenAI-SB: https://openai-sb.com/

配置方法：
```bash
# 在.env文件中添加
LLM_PROVIDER=openai
LLM_MODEL=gpt-3.5-turbo
OPENAI_API_KEY=从代理商获取的密钥
OPENAI_API_BASE=代理商提供的API地址
```

### 方案C：使用Claude（如果能获取正式API密钥）

如果您的 micu.wiki 服务提供标准的Claude API密钥（非cr_开头）：

```bash
# 在.env文件中配置
LLM_PROVIDER=claude
LLM_MODEL=claude-3-5-sonnet-20241022
ANTHROPIC_API_KEY=您的正式Claude密钥
ANTHROPIC_API_BASE=https://claude.micu.wiki
```

## 部署后的集成

### 在您的React前端中使用

修改 `client/src/services/agent.js`（新建）:

```javascript
const AGENT_API_BASE = 'http://8.136.43.216:8000/api/agent';

export class AgentService {
  constructor() {
    this.sessionId = this.getOrCreateSessionId();
  }

  getOrCreateSessionId() {
    let sessionId = sessionStorage.getItem('agentSessionId');
    if (!sessionId) {
      sessionId = 'user_' + Date.now();
      sessionStorage.setItem('agentSessionId', sessionId);
    }
    return sessionId;
  }

  async chat(message) {
    const response = await fetch(`${AGENT_API_BASE}/chat`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        message: message,
        session_id: this.sessionId
      })
    });
    return await response.json();
  }

  async getTools() {
    const response = await fetch(`${AGENT_API_BASE}/tools`);
    return await response.json();
  }
}

// 使用示例
const agent = new AgentService();
const result = await agent.chat('起飞到2米高度');
console.log(result.response);
```

### WebSocket实时通信

```javascript
const ws = new WebSocket('ws://8.136.43.216:8000/ws/chat');

ws.onopen = () => {
  console.log('Agent连接成功');
  ws.send(JSON.stringify({
    message: '你好',
    session_id: sessionStorage.getItem('agentSessionId')
  }));
};

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('Agent回复:', data.response);
};
```

## 常用运维命令

```bash
# SSH登录
ssh root@8.136.43.216

# 查看服务状态
cd /opt/agent-core
docker-compose -f docker-compose.prod.yml ps

# 查看实时日志
docker-compose -f docker-compose.prod.yml logs -f

# 重启服务
docker-compose -f docker-compose.prod.yml restart

# 停止服务
docker-compose -f docker-compose.prod.yml down

# 启动服务
docker-compose -f docker-compose.prod.yml up -d

# 更新服务（如果修改了代码）
docker-compose -f docker-compose.prod.yml build
docker-compose -f docker-compose.prod.yml up -d

# 查看资源占用
docker stats

# 清理Docker缓存（如果磁盘空间不足）
docker system prune -a
```

## 故障排查

### 问题1: 无法访问8000端口

**检查清单**:
```bash
# 1. 检查服务是否运行
docker ps | grep agent-core

# 2. 检查UFW防火墙
sudo ufw status
sudo ufw allow 8000/tcp

# 3. 检查端口监听
netstat -tlnp | grep 8000

# 4. 检查Docker日志
docker logs agent-core-prod
```

**最可能原因**: 阿里云安全组未开放8000端口
→ 去阿里云控制台配置安全组

### 问题2: API调用返回错误

```bash
# 查看详细日志
docker-compose -f docker-compose.prod.yml logs agent-core | tail -100

# 常见错误：
# - API key invalid: 检查.env中的API密钥是否正确
# - Connection refused: 检查BACKEND_URL是否正确
# - Out of memory: 服务器内存不足，考虑升级配置
```

### 问题3: 服务启动后自动停止

```bash
# 查看失败原因
docker-compose -f docker-compose.prod.yml logs

# 可能原因：
# - 依赖包安装失败: 重新build
# - 配置文件错误: 检查.env
# - 端口被占用: 修改端口或停止占用进程
```

## 监控和优化

### 资源监控

```bash
# 实时监控
docker stats agent-core-prod

# 建议配置资源限制
# 编辑 docker-compose.prod.yml，添加：
services:
  agent-core:
    mem_limit: 2g
    mem_reservation: 1g
    cpus: 1.5
```

### 日志管理

```bash
# 限制日志大小（编辑 docker-compose.prod.yml）
services:
  agent-core:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

## 成本预估

**月度成本**:
- 阿里云2核4GB服务器: ¥30-50/月
- OpenAI API (gpt-3.5-turbo): ¥10-30/月（轻度使用）
- **总计**: ¥40-80/月

**优化建议**:
- 使用阿里云包年优惠
- 控制API调用频率
- 启用对话历史缓存减少token消耗

---

## 快速命令清单

```bash
# 1. 上传文件
scp agent-core.tar.gz root@8.136.43.216:/opt/

# 2. 登录
ssh root@8.136.43.216

# 3. 部署
cd /opt && tar -xzf agent-core.tar.gz
cd agent-core && sudo bash quick-deploy.sh

# 4. 验证
curl http://localhost:8000/api/agent/health

# 5. 测试
curl -X POST http://8.136.43.216:8000/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"你好"}'
```

准备好后，按照上面的步骤开始部署吧！🚀
